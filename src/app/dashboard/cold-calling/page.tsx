import { ColdCallingWorkspace } from './components/ColdCallingWorkspace';

export const metadata = { title: 'Cold Calling — SigOS' };

export default function ColdCallingPage() {
  return <ColdCallingWorkspace />;
}
